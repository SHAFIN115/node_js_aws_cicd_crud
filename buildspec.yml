version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo Installing dependencies for build stage
      - npm install

  build:
    commands:
      - echo Build stage complete - skipping dev server

artifacts:
  files:
    - '**/*'